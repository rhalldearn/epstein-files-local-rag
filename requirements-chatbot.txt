# Epstein Files RAG Chatbot Dependencies
# For use with pip if not using UV

# Core dependencies
llama-cpp-python>=0.2.0
sentence-transformers>=2.2.0
chromadb>=0.4.0
pymupdf>=1.23.0
rich>=13.0.0
tqdm>=4.65.0
torch>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Note: For CUDA support with llama-cpp-python, install with:
# CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python --force-reinstall --no-cache-dir
